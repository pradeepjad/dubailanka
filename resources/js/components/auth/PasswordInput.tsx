import { Eye,EyeOff } from 'lucide-react'; import { useState } from 'react';
export default function PasswordInput({name,label,error,autoComplete='current-password'}:{name:string;label:string;error?:string;autoComplete?:string}){const [show,setShow]=useState(false);return <div><label className="mb-2 block text-sm font-semibold text-[#374151]">{label}</label><div className="relative"><input name={name} type={show?'text':'password'} autoComplete={autoComplete} className="w-full rounded-xl border border-[#d1d5db] px-4 py-3 pr-11 outline-none focus:border-[#005bff] focus:ring-4 focus:ring-[#005bff]/10"/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 text-[#6b7280]">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div>{error&&<p className="mt-2 text-sm text-[#ef2929]">{error}</p>}</div>}
