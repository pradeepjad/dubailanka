import { router,usePage } from '@inertiajs/react'; import AuthLayout from '../../components/auth/AuthLayout';
type P={auth?:{user?:{name:string;email:string;suspension_reason?:string|null}}};
export default function Suspended(){const {props}=usePage<P>();return <AuthLayout title="Account Restricted"><div className="rounded-2xl border border-[#fee2e2] bg-[#fff7f7] p-6"><p className="text-sm font-semibold text-[#ef2929]">Account restricted</p><h1 className="mt-2 text-3xl font-bold">Your account is suspended</h1><p className="mt-4 text-sm leading-6 text-[#6b7280]">{props.auth?.user?.suspension_reason||'Please contact Dubai Lanka support for more information.'}</p><button onClick={()=>router.post('/logout')} className="mt-6 rounded-xl bg-[#071426] px-5 py-3 text-sm font-semibold text-white">Log out</button></div></AuthLayout>}
